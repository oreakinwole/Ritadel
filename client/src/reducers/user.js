// import * as UserActionTypes from '../actiontypes/user';


export default function user(state = [], action) {
    console.log(action);
    switch(action.type) {

            default:
                return state;
    }
}